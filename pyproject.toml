[project]
name = "basic-socket-rpc"
version = "0.2.1"
description = "Basic binary socket based rpc for Python"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=7.0.1",
]
